CREATE TABLE `objects` (
    `id` INT NOT NULL AUTO_INCREMENT,
    `model` VARCHAR(255) NOT NULL,
    `x` FLOAT NOT NULL,
    `y` FLOAT NOT NULL,
    `z` FLOAT NOT NULL,
    `rx` FLOAT NOT NULL,
    `ry` FLOAT NOT NULL,
    `rz` FLOAT NOT NULL,
    PRIMARY KEY (`id`)
);
